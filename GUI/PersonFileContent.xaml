<Window x:Class="GUI.FileContent"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:GUI"
    xmlns:models="clr-namespace:SEM_2_CORE;assembly=SEM_2_CORE" 
    mc:Ignorable="d"
    Title="PeopleFileContent" Height="450" Width="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" MinHeight="34"/>
                <RowDefinition Height="Auto" MinHeight="34"/>
                <RowDefinition Height="Auto" MinHeight="34"/>
                <RowDefinition Height="Auto" MinHeight="34"/>
                <RowDefinition Height="Auto" MinHeight="34"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0">
                <Run Text="File path/name: " FontWeight="Bold"/><Run Text="{Binding Path}"/>
            </TextBlock>
            <TextBlock Grid.Row="1">
                <Run Text="Block size: " FontWeight="Bold"/><Run Text="{Binding BlockSize}"/>
            </TextBlock>
            <TextBlock Grid.Row="2">
                <Run Text="Block factor: " FontWeight="Bold"/><Run Text="{Binding BlockFactor}"/>
            </TextBlock>
            <TextBlock Grid.Row="3">
                <Run Text="Number of blocks: " FontWeight="Bold"/><Run Text="{Binding BlockCount}"/>
            </TextBlock>
            <ListBox x:Name="Block" Grid.Row="4" ItemsSource="{Binding Blocks}" Grid.ColumnSpan="1" Margin="5,3" VerticalAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:BlockViewData}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0">
                                <Run Text="Block number: " FontWeight="Bold"/><Run Text="{Binding BlockNumber}"/>
                            </TextBlock>
                            <TextBlock Grid.Row="1">
                                <Run Text="Records count: " FontWeight="Bold"/><Run Text="{Binding RecordsCount}"/>
                            </TextBlock>
                            <TextBlock Grid.Row="2">
                                <Run Text="Valid records: " FontWeight="Bold"/><Run Text="{Binding ValidCount}"/>
                            </TextBlock>
                            <TextBlock Grid.Row="3">
                                <Run Text="{Binding BlockData}"/>
                            </TextBlock>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </ScrollViewer>
</Window>
